repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.15.18
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format

# mypy runs in CI only (needs full project deps — pydantic, httpx, etc.)
# Local pre-commit hook runs ruff (fast, zero deps).
